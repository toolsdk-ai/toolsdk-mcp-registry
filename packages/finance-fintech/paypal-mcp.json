{
  "type": "mcp-server",
  "packageName": "@paypal/mcp",
  "description": "AI agent integration with PayPal services.",
  "url": "https://github.com/paypal/paypal-mcp-server",
  "runtime": "node",
  "license": "unknown",
  "binArgs": ["--tools=all"],
  "env": {
    "PAYPAL_ACCESS_TOKEN": {
      "description": "YOUR_PAYPAL_ACCESS_TOKEN",
      "required": true
    },
    "PAYPAL_ENVIRONMENT": {
      "description": "SANDBOX",
      "required": true
    }
  },
  "name": "Agent Toolkit MCP"
}
