{
  "type": "mcp-server",
  "packageName": "google-contacts-mcp",
  "description": "Allow AI systems to list, search, and manage contacts via Google Contacts.",
  "url": "https://github.com/domdomegg/google-contacts-mcp.git",
  "runtime": "node",
  "license": "unknown",
  "env": {
    "GOOGLE_ACCESS_TOKEN": {
      "description": "Google OAuth access token.",
      "required": true
    },
    "MCP_TRANSPORT": {
      "description": "Transport type.",
      "required": true
    },
    "GOOGLE_CLIENT_ID": {
      "description": "Google OAuth client ID.",
      "required": true
    },
    "GOOGLE_CLIENT_SECRET": {
      "description": "Google OAuth client secret.",
      "required": true
    }
  }
}
